%h1.d-flex.align-items-center.justify-content-center.mt-5
  GPT-3.5 turbo model
= form_with(model: @question) do |form|
  .row.mb-5
    .col
      = form.text_field :text, class: 'form-control border w-50 mx-auto', id: 'textAreaExample', required: true
    .d-flex.align-items-center.justify-content-center.mt-2
      = form.submit 'Ask', class: 'btn btn-primary'
.row.mt-4
  .col-sm-12.col-md-12
    - @questions.each do |question|
      .row.comment.my.mb-5
        .col-sm-12.mb-3
          %b
            = question.text
        .col-sm-12
          = question.response
